<%= form_for(@competency_pertenece_asignatura) do |f| %>
  <% if @competency_pertenece_asignatura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@competency_pertenece_asignatura.errors.count, "error") %> prohibited this competency_pertenece_asignatura from being saved:</h2>

      <ul>
      <% @competency_pertenece_asignatura.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :asignatura_id %><br />
    <%= f.number_field :asignatura_id %>
  </div>
  <div class="field">
    <%= f.label :competency_id %><br />
    <%= f.number_field :competency_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
